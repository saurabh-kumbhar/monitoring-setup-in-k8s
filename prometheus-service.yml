apiVersion: v1

kind: Service

metadata:
  name: prometheus-service
  labels:
    name: prometheus-service
    app: basic-monitoring-app

spec:
  # type: LoadBalancer
  type: NodePort
  ports:
    - port: 9090
      targetPort: 80
  selector:
    name: prometheus-pod
    app: basic-monitoring-app