apiVersion: v1

kind: Service

metadata:
  name: grafana-service
  labels:
    name: grafana-service
    app: basic-monitoring-app

spec:
  # type: LoadBalancer
  type: NodePort
  ports:
    - port: 3000
      targetPort: 80
  selector:
    name: grafana-pod
    app: basic-monitoring-app